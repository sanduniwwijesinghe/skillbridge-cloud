apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service
  namespace: skillbridge
spec:
  replicas: 1
  selector:
    matchLabels: { app: booking-service }
  template:
    metadata:
      labels: { app: booking-service }
    spec:
      containers:
      - name: booking-service
        image: ap-south-1.amazonaws.com/sanduniwijesinghe/booking-service:latest
        ports:
        - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: skillbridge
spec:
  selector:
    app: booking-service
  ports:
  - port: 80
    targetPort: 8000
  type: ClusterIP
